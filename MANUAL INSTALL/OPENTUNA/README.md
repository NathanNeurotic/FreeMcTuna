Select ONLY the corresponding OpenTuna .psu file which applies to yourspecific Console's ROM Version.
copy the .psu with wlaunchelf's file explorer, then psupaste it to the root of your chosen memory card.
Run BIOS.ELF to check your version.
